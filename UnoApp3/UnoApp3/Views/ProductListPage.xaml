<Page x:Class="UnoApp3.Views.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:vm="using:UnoApp3.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:ProductListViewModel}"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with search and cart button -->
        <utu:NavigationBar Content="Danh sách sản phẩm" Grid.Row="0"/>

        <Grid Grid.Row="1" Padding="16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     PlaceholderText="Tìm kiếm sản phẩm..."
                     Text="{Binding SearchText, Mode=TwoWay}"
                     Margin="0,0,12,0"/>

            <Button Grid.Column="1"
                    Command="{Binding LoadProductsCommand}"
                    Margin="0,0,8,0">
                <SymbolIcon Symbol="Refresh"/>
            </Button>

            <Button Grid.Column="2"
                    Command="{Binding GoToCartCommand}">
                <SymbolIcon Symbol="Shop"/>
            </Button>
        </Grid>

        <!-- Tab filter (Category/Supplier - displayed as Pivot) -->
        <Pivot Grid.Row="1" Margin="0,10,0,0">
            <PivotItem Header="Tất cả">
                <Grid>
                    <!-- Product List with Refresh (if RefreshContainer works) -->
                    <ListView ItemsSource="{Binding Products}"
                              SelectionMode="None"
                              HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="12"
                                      Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Product Image -->
                                    <Border Grid.Column="0"
                                            Width="80"
                                            Height="80"
                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                            CornerRadius="8">
                                        <Image Source="{Binding ImageUrl}"
                                               Stretch="UniformToFill"/>
                                    </Border>

                                    <!-- Product Details -->
                                    <StackPanel Grid.Column="1"
                                                Margin="12,0"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ProductName}"
                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding CategoryName}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding PriceFormatted}"
                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                   Foreground="{ThemeResource SystemAccentColor}"
                                                   Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding InventoryFormatted}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Add to Cart Button -->
                                    <Button Grid.Column="2"
                                            Command="{Binding DataContext.AddToCartCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            VerticalAlignment="Center"
                                            Background="Transparent">
                                        <SymbolIcon Symbol="Add"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>

            <!-- Can add other PivotItems for category here -->
            <PivotItem Header="Theo danh mục">
                <TextBlock Text="Sẽ implement sau"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </PivotItem>
        </Pivot>

        <!-- Progress Ring when loading -->
        <ProgressRing Grid.Row="1"
                      IsActive="{Binding IsBusy}"
                      Width="50"
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</Page>
