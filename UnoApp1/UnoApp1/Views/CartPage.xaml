<Page x:Class="UnoApp1.Views.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:utum="using:Uno.Toolkit.UI.Material"
      xmlns:entities="using:UnoApp1.Data.Entities"
      xmlns:themes="clr-namespace:Uno.Themes;assembly=Uno.Themes.WinUI">

    <Grid utu:SafeArea.Insets="All"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Cart Items -->
        <ScrollViewer Grid.Row="0">
            <StackPanel Spacing="8" Padding="16">

                <!-- Empty State -->
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsEmpty, Converter={StaticResource TrueToVisibleConverter}}">
                    <FontIcon Glyph="&#xE719;"
                              FontSize="64"
                              Foreground="{ThemeResource OnSurfaceVariantBrush}"
                              Margin="0,0,0,16" />
                    <TextBlock Text="Your cart is empty"
                               FontSize="18"
                               Foreground="{ThemeResource OnSurfaceVariantBrush}"
                               TextAlignment="Center" />
                    <Button Content="Continue Shopping"
                            Command="{Binding ContinueShoppingCommand}"
                            Style="{StaticResource TextButtonStyle}"
                            Margin="0,16,0,0" />
                </StackPanel>

                <!-- Cart Items -->
                <ItemsControl ItemsSource="{Binding CartItems}"
                              Visibility="{Binding IsEmpty, Converter={StaticResource FalseToVisibleConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="entities:CartItem">
                            <Border Background="{ThemeResource SurfaceBrush}"
                                    CornerRadius="8"
                                    Margin="0,4"
                                    Padding="12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Item Details -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{x:Bind ProductName}"
                                                   FontWeight="SemiBold"
                                                   FontSize="16" />
                                        <TextBlock Text="{x:Bind CategoryName}"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                                   Margin="0,4,0,0" />
                                        <TextBlock
                                            Text="{x:Bind Price, Converter={StaticResource StringFormatConverter}, ConverterParameter='${0:F2} each'}"
                                            FontSize="14"
                                            Margin="0,8,0,0" />
                                    </StackPanel>

                                    <!-- Quantity Controls -->
                                    <StackPanel Grid.Column="1"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center">
                                        <TextBlock
                                            Text="{x:Bind TotalPrice, Converter={StaticResource StringFormatConverter}, ConverterParameter='${0:F2}'}"
                                            FontWeight="Bold"
                                            FontSize="18"
                                            Foreground="{ThemeResource PrimaryBrush}"
                                            HorizontalAlignment="Right" />

                                        <Grid Margin="0,8,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0"
                                                    Content="-"
                                                    Command="{Binding DataContext.UpdateQuantityCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    CommandParameter="{x:Bind}"
                                                    Style="{StaticResource OutlinedButtonStyle}"
                                                    Width="32"
                                                    Height="32" />

                                            <TextBlock Grid.Column="1"
                                                       Text="{x:Bind Quantity}"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Margin="12,0"
                                                       MinWidth="20" />

                                            <Button Grid.Column="2"
                                                    Content="+"
                                                    Command="{Binding DataContext.UpdateQuantityCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    CommandParameter="{x:Bind}"
                                                    Style="{StaticResource OutlinedButtonStyle}"
                                                    Width="32"
                                                    Height="32" />
                                        </Grid>

                                        <Button Content="Remove"
                                                Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{x:Bind}"
                                                Style="{StaticResource TextButtonStyle}"
                                                Margin="0,4,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Checkout Panel -->
        <Border Grid.Row="1"
                Background="{ThemeResource SurfaceBrush}"
                BorderBrush="{ThemeResource OutlineBrush}"
                BorderThickness="1,1,0,0"
                Padding="16"
                Visibility="{Binding IsEmpty, Converter={StaticResource FalseToVisibleConverter}}">
            <StackPanel Spacing="12">
                <!-- Summary -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Total Items"
                                   FontSize="14"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                        <TextBlock Text="{Binding TotalItems}"
                                   FontSize="16"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="Total Amount"
                                   FontSize="14"
                                   Foreground="{ThemeResource OnSurfaceVariantBrush}" />
                        <TextBlock
                            Text="{Binding TotalAmount, Converter={StaticResource StringFormatConverter}, ConverterParameter='${0:F2}'}"
                            FontSize="20"
                            FontWeight="Bold"
                            Foreground="{ThemeResource PrimaryBrush}" />
                    </StackPanel>
                </Grid>

                <!-- Checkout Button -->
                <Button Content="Confirm Purchase"
                        Command="{Binding ConfirmPurchaseCommand}"
                        Style="{StaticResource FilledButtonStyle}"
                        HorizontalAlignment="Stretch">
                    <themes:ControlExtensions.Icon>
                        <SymbolIcon Symbol="Shop" />
                    </themes:ControlExtensions.Icon>
                </Button>
            </StackPanel>
        </Border>

        <!-- Loading Indicator -->
        <ProgressRing IsActive="{Binding IsBusy}"
                      Visibility="{Binding IsBusy, Converter={StaticResource TrueToVisibleConverter}}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center" />
    </Grid>
</Page>
