<Page
    x:Class="UnoApp2.Presentation.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoApp2.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d"

    xmlns:vm="using:UnoApp2.Presentation"
    d:DataContext="{d:DesignInstance vm:Page1ViewModel, IsDesignTimeCreatable=True}"

    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Padding="20" Spacing="10" Background="#F0F0F0">
            <TextBlock Text="{Binding Title}"
                       FontWeight="Bold" Margin="0,0,0,10" />
            <Button Content="Filled" Style="{ThemeResource FilledButtonStyle}" />
            <Button Content="Elevated" Style="{ThemeResource ElevatedButtonStyle}" />
            <Button Content="Outlined" Style="{ThemeResource OutlinedButtonStyle}" />
            <Button Content="Text" Style="{ThemeResource TextButtonStyle}" />
            <Button Content="Icon" Style="{ThemeResource IconButtonStyle}" />
            <Button Content="FAB" Style="{ThemeResource FabStyle}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Padding="40" Spacing="30" HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Current Counter Value:" FontSize="24" />
            <TextBlock Text="{Binding Counter}"
                       FontSize="72"
                       FontWeight="ExtraBold"
                       HorizontalAlignment="Center" />

            <Button Content="+10 Async"
                    Command="{Binding IncreaseByTenCommand}"
                    FontSize="24"
                    Padding="40,10"
                    Style="{ThemeResource FilledButtonStyle}" />
        </StackPanel>
    </Grid>
</Page>
